/**
 * ARQUIVO DE EXEMPLO PARA PRODUÇÃO - NÃO CONTÉM API KEYS REAIS
 * 
 * Para usar em produção:
 * 1. Copie este arquivo para environment.prod.ts
 * 2. Substitua 'YOUR_API_KEY_HERE' pela sua API key real do OpenWeatherMap
 * 3. NUNCA commite o arquivo environment.prod.ts com a key real!
 */
export const environment = {
  production: true,
  // API Keys para serviços externos
  weatherApiKey: 'YOUR_API_KEY_HERE' // ⚠️ SUBSTITUA PELA SUA API KEY REAL
};

