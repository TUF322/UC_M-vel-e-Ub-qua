// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

/**
 * ARQUIVO DE EXEMPLO - NÃO CONTÉM API KEYS REAIS
 * 
 * Para usar este projeto:
 * 1. Copie este arquivo para environment.ts
 * 2. Substitua 'YOUR_API_KEY_HERE' pela sua API key real do OpenWeatherMap
 * 3. NUNCA commite o arquivo environment.ts com a key real!
 * 
 * Obtenha sua API key gratuita em: https://home.openweathermap.org/users/sign_up
 */
export const environment = {
  production: false,
  // API Keys para serviços externos
  weatherApiKey: 'YOUR_API_KEY_HERE' // ⚠️ SUBSTITUA PELA SUA API KEY REAL
};


